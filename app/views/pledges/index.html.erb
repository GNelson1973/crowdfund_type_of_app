<header id="content-header">
  <h1>Pledges for <%= link_to(@project.name, @project) %></h1>
</header>

<ol id="pledges">
  <% @pledges.each do |pledge| %>
  <li>
    <%= mail_to(pledge.email, pledge.name.capitalize) %>
    (<%= number_to_currency(pledge.pledge_amount, :unit => "EUR ") %>)
  </li>
  <% end %>
</ol>
